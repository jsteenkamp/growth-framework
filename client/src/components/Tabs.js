import React from 'react';
import Flex from './Flex';

const Tabs = props => <Flex bg="backgroundColor" borderBottom="1px solid" borderColor="primaryColor" {...props} />;

Tabs.displayName = 'Tabs';

export default Tabs;
